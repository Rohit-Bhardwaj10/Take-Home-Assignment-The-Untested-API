services:
  - type: web
    name: task-api
    env: node
    buildCommand: npm install
    startCommand: node src/app.js
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
